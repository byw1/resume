{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "//": "Docker rather than Nixpacks because server-side PDF rendering needs a Chromium in the image, and Railway's default builder has none. See the comment at the top of Dockerfile for how to go back.",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npx next start -p ${PORT:-3000}",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
